extends ./subSubTemplate.jade

block subSubTitle
	h1
		b Derivatives

block subSubContent
	h3
		b Differential
	p When we change the value of an input to a function, we also change the output. We can examine these changes.
	p Consider the value of a function \(f(x)\) at points \(x_1\) and \(x_2\).
	$$y_1=f(x_1)$$
	$$y_2=f(x_2)$$
	$$y_2-y_1=f(x_2)-f(x_1)$$
	$$\frac{y_2-y_1}{x_2-x_1}=\frac{f(x_2)-f(x_1)}{x_2-x_1}$$
	p Let's define \(x_2\) in terms of its distance from x_1:
	$$x_2=x_1+\epsilon$$
	$$\frac{y_2-y_1}{\epsilon }=\frac{f(x_1+\epsilon )-f(x_1)}{\epsilon }$$
	p We define the differential of a function as:
	$$\frac{\delta y}{\delta x}=\lim_{\epsilon \rightarrow 0^+}\frac{f(x+\epsilon )-f(x)}{\epsilon }$$
	p If this is defined, then we say the function is differentiable at that point.
	p
		b L'Hopital
	p If there are two functions which are both tend to \(0\) at a limit, calculating the limit of their divisor is hard. We can use L'Hopital's rule.
	p We want to calculate:
	p $$\lim_{x\rightarrow c}\frac{f(x)}{g(x)}$$
	p This is:
	p $$\lim_{x\rightarrow c}\frac{f(x)}{g(x)}=\lim_{x\rightarrow c}\frac{\frac{f(x)-0}{\delta}}{\frac{g(x)-0}{\delta}}$$
	p If:
	p $$\lim_{x\rightarrow c}f(x)=\lim_{x\rightarrow c}g(x)=0$$
	p Then
	p $$\lim_{x\rightarrow c}\frac{f(x)}{g(x)}=\lim_{x\rightarrow c}\frac{\frac{f(x)-f(c)}{\delta}}{\frac{g(x)-f(c)}{\delta}}$$
	p $$\lim_{x\rightarrow c}\frac{f(x)}{g(x)}=\frac{f'(x)}{g'(x)}$$
