extends ./subSubTemplate.jade

block subSubTitle
	h1
		b Dual space

block subSubContent
	h3
		b Linear forms
	p A linear form is a linear function which maps a vector onto its underlying field.
	p A linear form is a special type of linear map.
	h3
		b Dual space
	p For a vector space \(V\), its dual space is \(Hom(V,F)\), the morphism from itself to its underlying field.
	p It is the set of all linear forms.
