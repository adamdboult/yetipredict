extends ./subSubTemplate.jade
block subSubTitle
	h1
		b Dot product
block subSubContent
	h3
		b Introduction
	p Given two vectors we can calculate:
	p \(\frac{a.b}{||a|| ||b||}\)
	p If the two vectors are identical, this is \(1\). If they are orthogonal this is \(0\). If they are opposite, this is \(-1\).
